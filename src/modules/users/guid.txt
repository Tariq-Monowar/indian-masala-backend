=================================
create admin
______________POST
{{_baseUrl}}/api/users/create-admin

header: {
}

query: {
}

body: {
  - name: string
  - email: string
  - password: string
  - image: file
}

note:
  - creates a user with role admin
  - email must be unique
  - image is optional
  - Content-Type is multipart/form-data when sending image
  - saved image is filename only

Response: {
  {
    "success": true,
    "user": {
      "id": "uuid",
      "name": "Tariq",
      "email": "tariq@example.com",
      "image": "1710000000000-avatar.png",
      "role": "admin"
    }
  }
}

=================================
admin login
______________POST
{{_baseUrl}}/api/users/admin-login

header: {
}

query: {
}

body: {
  - email: string
  - password: string
}

note:
  - login with email and password
  - token payload is id, email, role
  - token has no expiresIn and no exp

Response: {
  {
    "success": true,
    "token": "jwt"
  }
}

=================================
customer login
______________POST
{{_baseUrl}}/api/users/customer-login

header: {
}

query: {
}

body: {
  - email: string
}

note:
  - public route, no password
  - email must already have an order
  - token payload is name, email, phone, role: customer
  - same token used on create order and get all orders

Response: {
  {
    "success": true,
    "token": "jwt"
  }
}

=================================
forgotpassword sendotp
______________POST
{{_baseUrl}}/api/users/forgotpassword/sendotp

header: {
}

query: {
}

body: {
  - email: string
}

note:
  - user must already exist
  - OTP is stored in redis for 5 minutes
  - otp is returned only when NODE_ENV is development

Response: {
  {
    "success": true,
    "message": "OTP sent to your email",
    "otp": null
  }
}

=================================
forgotpassword verifyotp
______________POST
{{_baseUrl}}/api/users/forgotpassword/verifyotp

header: {
}

query: {
}

body: {
  - email: string
  - otp: string
}

note:
  - verify the OTP from sendotp
  - after verify, reset is allowed for 10 minutes

Response: {
  {
    "success": true,
    "message": "OTP verified successfully"
  }
}

=================================
forgotpassword reset
______________POST
{{_baseUrl}}/api/users/forgotpassword/reset

header: {
}

query: {
}

body: {
  - email: string
  - password: string
}

note:
  - call after verifyotp
  - permission_to_update_password must be true

Response: {
  {
    "success": true,
    "message": "Password reset successfully!"
  }
}

=================================
forgotpassword recentotp
______________POST
{{_baseUrl}}/api/users/forgotpassword/recentotp

header: {
}

query: {
}

body: {
  - email: string
}

note:
  - resend OTP for an active forgot-password session
  - session must still exist in redis

Response: {
  {
    "success": true,
    "message": "New OTP sent successfully",
    "otp": null
  }
}

=================================
change password
______________POST
{{_baseUrl}}/api/users/change-password

header: {
  - token: Admin
}

query: {
}

body: {
  - current_password: string
  - new_password: string
}

note:
  - send jwt in token header
  - current_password must match the logged in user
  - new_password replaces the old password

Response: {
  {
    "success": true,
    "message": "Password changed successfully"
  }
}

=================================
check auth
______________GET
{{_baseUrl}}/api/users/check-auth

header: {
  - token: Admin
}

query: {
}

body: {
}

note:
  - send login jwt in token header
  - const { id } = request.user
  - returns the logged in user
  - missing or invalid user returns unauthorized

Response: {
  {
    "success": true,
    "data": {
      "name": "Tariq",
      "email": "tariq@example.com",
      "phone": null,
      "image": "1710000000000-avatar.png",
      "role": "admin",
      "createdAt": "2026-09-12T09:00:00.000Z"
    }
  }

  {
    "success": false,
    "message": "Unauthorized"
  }
}
